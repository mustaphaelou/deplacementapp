import { auth } from "@/lib/auth"; import { redirect } from "next/navigation"; import { demandeService } from "@/lib/demande-service"; import type { StatutDemande } from "@prisma/client"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { formatCurrency, STATUT_LABELS } from "@/lib/constants"; import { hasAnyRole } from "@/lib/authorization"; import Link from "next/link"; import { FileText, Users, TrendingUp, CheckCircle, XCircle, Clock } from "lucide-react"; export default async function RapportsPage() {  const session = await auth()  const userRole = session?.user?.role  if (!hasAnyRole(userRole ?? "", ["FINANCE_ADMIN", "GENERAL_DIRECTION"])) {    redirect("/")  }  const statutCounts = await Promise.all(    Object.keys(STATUT_LABELS).map(async (statut) => ({      statut,      label: STATUT_LABELS[statut],      count: await demandeService.queries.countByStatut(statut as StatutDemande),    }))  )  const totalDemandes = statutCounts.reduce((sum, s) => sum + s.count, 0)  const totalApprouvees = statutCounts.find((s) => s.statut === "APPROUVEE")?.count ?? 0  const totalRejetees = (["REJETEE_MANAGER", "REJETEE_FINANCE", "REJETEE_DIRECTION"] as const).reduce(    (sum, statut) => sum + (statutCounts.find((s) => s.statut === statut)?.count ?? 0),    0  )  const totalBudget = await demandeService.queries.aggregateBudget([    "APPROUVEE",    "APPROUVEE_FINANCE",    "APPROUVEE_MANAGER",  ])  return (    <div className="space-y-6">      <div>        <h1 className="text-2xl font-bold">Rapports</h1>        <p className="text-sm text-muted-foreground">Vue d'ensemble des demandes de déplacement</p>      </div>      <div className="grid gap-4 md:grid-cols-4">        <StatCard icon={FileText} label="Total demandes" value={totalDemandes} />        <StatCard icon={CheckCircle} label="Approuvées" value={totalApprouvees} />        <StatCard icon={XCircle} label="Rejetées" value={totalRejetees} />        <StatCard icon={TrendingUp} label="Budget total" value={formatCurrency(totalBudget)} />      </div>      <Card>        <CardHeader>          <CardTitle className="text-base">Répartition par statut</CardTitle>        </CardHeader>        <CardContent>          <div className="space-y-3">            {statutCounts.map((s) => (              <div key={s.statut} className="flex items-center justify-between text-sm">                <span>{s.label}</span>                <span className="font-medium">{s.count}</span>              </div>            ))}          </div>        </CardContent>      </Card>      <div className="flex gap-3">        <Link          href="/api/csv"          className="inline-flex items-center rounded-lg border bg-background px-4 py-2 text-sm font-medium hover:bg-muted"        >          <FileText className="mr-2 size-4" />          Exporter en CSV        </Link>      </div>    </div>  )}function StatCard({ icon: Icon, label, value }: { icon: any; label: string; value: string | number }) {  return (    <Card>      <CardContent className="flex items-center gap-4 p-5">        <div className="bg-primary/10 flex size-10 items-center justify-center rounded-lg">          <Icon className="text-primary size-5" />        </div>        <div>          <p className="text-2xl font-bold">{value}</p>          <p className="text-xs text-muted-foreground">{label}</p>        </div>      </CardContent>    </Card>  )}